# Bark 配置（必填）
BARK_KEY=YOUR_DEVICE_KEY

# Bark 服务器地址（可选，默认为官方服务器）
# BARK_BASE=https://api.day.app

# 监控步长（可选）
BTC_STEP=1000
ETH_STEP=100
RATIO_STEP=0.5

# 轮询间隔，单位：秒（可选）
INTERVAL=30

# SQLite数据库路径（可选，Docker环境默认为/app/data/price_history.db）
# DB_PATH=/app/data/price_history.db

